[package]
name = "client"
version = "0.6.0"
edition.workspace = true
rust-version.workspace = true
description.workspace = true
authors.workspace = true
publish.workspace = true
build = "build.rs"
default-run = "client"

[[bin]]
name = "client"
path = "src/main.rs"

# [[bin]]
# name = "shadow_tool"
# path = "src/shadow_tool.rs"


[dependencies]
ahash = { workspace = true }
bitflags = { workspace = true }
bytemuck = { workspace = true }
chrono = { workspace = true }
constcat = { workspace = true }
dashmap = { workspace = true }
egui = { workspace = true }
glam = { workspace = true }
half = { workspace = true }
hecs = { workspace = true }
lazy_static = { workspace = true }
log = { workspace = true }
parking_lot = { workspace = true }
rayon = { workspace = true }
serde = { workspace = true }
serde_json = { workspace = true }
spin = { workspace = true }
static_assertions = { workspace = true }
thiserror = { workspace = true }
tracing = { workspace = true }
tracing-subscriber = { workspace = true }
tracing-appender = { workspace = true }
wgpu = { workspace = true }
winit = { workspace = true }

# DirectDraw Surface file format parser/composer
# License: MIT
ddsfile = "0.5"

# Imaging library. Provides basic image processing and encoders/decoders for common image formats.
# License: MIT OR Apache-2.0
image = { version = "0.25", features = ["dds", "png", "tga"], default-features = false }

# 자체 제작 프레임워크 
mod-app = { path = "../mod-app" }
mod-network = { path = "../mod-network" }
mod-parallelism = { path = "../mod-parallelism" }
mod-physics = { path = "../mod-physics" }
mod-render = { path = "../mod-render" }


# 사용 플랫폼: Windows, macOS
[target.'cfg(any(target_os = "windows", target_os = "macos"))'.dependencies]
# Synchronously block the thread until a future completes
# License: Apache-2.0 OR MIT
pollster = "0.4"

[build-dependencies]
serde = { workspace = true }
serde_json = { workspace = true }

# Expanding std::fs and std::io. Recursively copy folders with information about process and much more.
# License: MIT
fs_extra = "1.3"



[features]
default = ["enable-shader-validation"]

# 쉐이더 생성시 유효성 검사를 활성화합니다.
enable-shader-validation = []

# 캐릭터의 변환 행렬을 출력할 때 사용합니다.
print-transform = []
